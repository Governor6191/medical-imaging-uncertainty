# Dependencies for the BraTS Hugging Face Space (CPU). The Space installs these,
# then runs app.py. Torch comes from PyPI here (CPU wheels), unlike the CUDA pins
# in pyproject.toml that target a local RTX 4070.
torch
torchvision
timm
segmentation_models_pytorch
torchmetrics
albumentations
numpy
pillow
matplotlib
omegaconf
huggingface_hub
gradio
